[package]
name = "kindra"
version = "0.2.2"
description = "CLI for managing stacked Git branches and related review workflows"
license = "MIT"
edition = "2024"
rust-version = "1.88"
repository = "https://github.com/Pajn/kindra"
homepage = "https://github.com/Pajn/kindra"
documentation = "https://docs.rs/kindra/0.2.2/kindra/"
readme = "README.md"
keywords = ["git", "cli", "stacked-branches", "workflow", "pull-request"]
categories = ["command-line-utilities", "development-tools"]

[lib]
name = "kindra"

[[bin]]
name = "kin"
path = "src/main.rs"

[dependencies]
base64 = "0.22"
clap = { version = "4.5", features = ["derive"] }
clap_complete = { version = "4.5", features = ["unstable-dynamic"] }
clap_complete_nushell = "4.5"
git2 = { version = "0.20", features = ["vendored-libgit2", "vendored-openssl"] }
tempfile = "3.10"
anyhow = "1.0"
inquire = "0.7"
crossterm = "0.28"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
dirs = "6.0"
fs2 = "0.4"
git-absorb = "0.9"
slog = "2.7"
slog-term = "2.9"

[target.'cfg(unix)'.dependencies]
rustix = { version = "1.1.4", features = ["process"] }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"
regex = "1"
which = "6.0"
criterion = "0.5"

[[bench]]
name = "checkout_top"
harness = false

[[bench]]
name = "restack_discovery"
harness = false

[[bench]]
name = "sync"
harness = false

[package.metadata.binstall]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ target }{ archive-suffix }"
bin-dir = "{ bin }{ binary-ext }"
pkg-fmt = "tgz"

[package.metadata.binstall.overrides.'cfg(target_os = "windows")']
pkg-fmt = "zip"
